<h4 class="settings__subsection-title">アカウント情報編集</h4>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :name, "名前" %><br>
    <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
  </div>

  <div class="field">
    <%= f.label :email, "メールアドレス" %><br>
    <%= f.email_field :email, autocomplete: "email" %>
  </div>

  <hr>

  <p>パスワードを変更する場合は、以下に入力してください（変更しない場合は空欄でOK）</p>

  <div class="field">
    <%= f.label :password, "新しいパスワード" %> <i>(6文字以上)</i><br>
    <%= f.password_field :password, autocomplete: "new-password", placeholder: "新しいパスワード" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, "パスワード確認" %><br>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "確認用パスワード" %>
  </div>

  <div class="field">
    <%= f.label :current_password, "現在のパスワード" %> <i>(必須)</i><br>
    <%= f.password_field :current_password, autocomplete: "current-password", placeholder: "現在のパスワード" %>
  </div>

  <div class="form-actions-buttons">
    <%= link_to "削除", registration_path(resource_name), data: { confirm: "本当に削除しますか？" }, method: :delete, class: "btn btn-danger" %>
    <%= f.submit "変更を保存", class: "btn btn-primary" %>
  </div>
<% end %>